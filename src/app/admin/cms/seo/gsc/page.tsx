import GscDashboardPage, { metadata as pageMetadata } from "@/app/admin/seo/gsc/page";

export const dynamic = "force-dynamic";
export const metadata = pageMetadata;

export default GscDashboardPage;
